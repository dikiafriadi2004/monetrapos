name: Member Admin CI/CD

on:
  push:
    branches: [main, develop]
    paths:
      - 'apps/member-admin/**'
      - '.github/workflows/member-admin.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'apps/member-admin/**'

jobs:
  test:
    name: Lint & Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/member-admin/package-lock.json

      - name: Install dependencies
        working-directory: apps/member-admin
        run: npm ci

      - name: Lint
        working-directory: apps/member-admin
        run: npm run lint -- --max-warnings 50
        continue-on-error: true

      - name: Type check
        working-directory: apps/member-admin
        run: npx tsc --noEmit
        continue-on-error: true

      - name: Build
        working-directory: apps/member-admin
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:4404/api/v1

  deploy-staging:
    name: Deploy to Staging
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4
      - name: Deploy member-admin to staging
        run: echo "Deploy member-admin to staging - configure with Vercel/Netlify/custom"

  deploy-production:
    name: Deploy to Production
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4
      - name: Deploy member-admin to production
        run: echo "Deploy member-admin to production - configure with Vercel/Netlify/custom"
